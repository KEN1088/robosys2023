name: test  #ワークフローの名前
on: push  #いつこのフローを走らせるか（今回はプッシュしたとき）
jpbs: #走らせたい処理のリスト
        test: 
                runs-on: ubuntu-latest
                steps:
                - uses: actions/checkout@v3
                - name: All test
                  run: bash -xv ./test.bash

